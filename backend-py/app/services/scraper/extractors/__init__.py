"""Per-field extractors. Each module exposes ``async def extract(html, url) -> dict``.

These are TODO stubs — the Node extractors (~13K lines across course-name,
fee, IELTS, PTE, TOEFL, intake, duration, study-mode, academic, scholarship,
delivery-mode, location) need to be ported one-by-one. The interface below
is what the orchestrator and tests will call.
"""
