# Royal Holloway University
# Hostname: www.royalholloway.ac.uk
# Country: United Kingdom  |  Currency: GBP
# Slug: royalholloway
#
# Course URL pattern: /studying-here/(undergraduate|postgraduate|central-london-campus)/<dept>/<course-slug>
# Research degrees are under /studying-here/postgraduate/ (e.g. /postgraduate/mathematics/mathematics-phd)
#
# Discovery notes:
#   - /studying-here/ root page finds ~46 course links directly, but 2 PG courses are
#     only reachable via department sub-pages (e.g. /postgraduate/classics/ancient-history).
#     BFS budget raised to 50 so it can visit more dept sub-pages before hitting the cap.

discovery:
  bfs_page_budget: 50

  # Only accept 4-segment course paths under studying-here.
  # Blocks /ug-prospectus, /pg-prospectus, /fees-and-funding/*, etc.
  allow_url_patterns:
    - 'royalholloway\.ac\.uk/studying-here/(undergraduate|postgraduate|central-london-campus)/[^/]+/[^/]+'

extraction:
  fees:
    default_currency: GBP
  filters:
    # Disabled: study_mode extractor reads "Online" from page text at conf=0.50
    # on courses that are actually On Campus — causes false online_only rejections.
    online_only:
      enabled: false
  staging:
    reject_if_missing:
      - course_name
    skip_degree_qualifier_check: true
    require_international_fee: false
