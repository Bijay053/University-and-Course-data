# ─────────────────────────────────────────────────────────────────────────────
# DEAD FILE — THIS CONFIG IS NEVER LOADED
# ─────────────────────────────────────────────────────────────────────────────
#
# Hostname: www.unisc.edu.au
# Derived slug: "unisc"  (www. stripped → "unisc")
#
# The loader looks for:
#   1. unisc_2235.yaml  (id-specific, highest priority)
#   2. unisc.yaml       (slug fallback)
#
# "usc" does NOT match any of these.  All UniSC config changes must be made
# in unisc_2235.yaml.  This file is kept as a breadcrumb only.
