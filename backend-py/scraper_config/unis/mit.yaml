# ─── AUTO-GENERATED YAML for mit ───────────────────
# Source:    csu.yaml
# Generated: 2026-05-13 06:50 UTC
# Generator: yaml_cascade.run_cascade_for_university()
#
# This file was cloned from the highest-scoring existing per-uni
# YAML based on CMS-family + region + URL-shape signals. It is a
# starting stub, NOT a finished tuning. After the next scrape:
#   1. Compare staged completeness vs the source uni.
#   2. Refine fee/intake/IELTS extractors per the per-uni-fix
#      convention documented in replit.md.
#   3. Delete this header once the file is hand-reviewed.
#
# target_university_id=11
# target_university_name=MIT
# target_url=https://www.mit.edu.au/study-with-us/international-students/entry-requirements
# target_families=['drupal']
# target_region=au
# chosen_candidate=csu (score=9.73)
# ─────────────────────────────────────────────────────────────

# Charles Sturt University
# Hostname: www.csu.edu.au  (now possibly www.charlesturt.edu.au post-rebrand)
#
# Bug history / rationale:
#   - SPA browser discovery: CSU's course listing is a React/Next.js SPA.
#     BFS static HTML sees only the shell HTML; actual course links are loaded
#     via XHR.  A browser discovery pass (playwright) is needed.
#   - 'Actual rows mismatch' bug: the number of courses staged does not match
#     the count shown on the CSU website.  Root cause may be pagination (BFS
#     misses pages 2+) or the SPA rendering issue above.
#   - CSU rebranded to 'Charles Sturt University' but domain may still use csu.
#
# TODO: confirm current hostname (csu.edu.au vs charlesturt.edu.au).
# TODO: investigate whether csu_browser_discover.py already handles the SPA.

discovery:
  always_sitemap_supplement: true   # Supplement BFS with sitemap to catch SPA-only URLs

extraction:
  filters:
    domestic_only:
      enabled: true   # Preserve current prod behaviour: filter runs for all unis.

    online_only:
      # POLICY CHANGE 2026-05-30: online-only filter re-enabled globally.
      # International students on student visas require on-campus delivery.
      enabled: true
