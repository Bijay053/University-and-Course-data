kind = "api"
previewPath = "/api" # TODO - should be excluded from preview in the first place
title = "API Server (REMOVED — Python FastAPI only)"
version = "1.0.0"
id = "3B4_FFSkEVBkAeYMFRJ2e"

[[services]]
localPort = 8080
name = "API Server"
paths = ["/api"]

[services.development]
# Node API server has been DELETED. Python FastAPI (backend-py: FastAPI)
# is now the sole API server on port 8080 for all /api/* routes.
# Both in Replit dev and on DigitalOcean production.
run = "echo 'Node API server deleted — Python FastAPI on :8080 is the sole API server. See replit.md.'; exit 0"
