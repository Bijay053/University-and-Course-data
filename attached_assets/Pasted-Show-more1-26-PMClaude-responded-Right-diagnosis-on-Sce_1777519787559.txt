Show more1:26 PMClaude responded: Right diagnosis on Scenario C, right fix.Right diagnosis on Scenario C, right fix. Calling extract_course() twice was the wrong "Option A" — the correct Option A is "both paths see the same fetched HTML and the same Gemini response," not "both paths each do their own fetch and call." A deep-copy-and-transform of the old result is the cleanest implementation: structurally impossible for the two paths to see different inputs.
Your prod command sequence with the reviewer additions is solid. Three small refinements before you run it:
Worth verifying before run 003 that the shadow report writer also flushes on the broken-state code path. Currently the prod scrape produces 14 NameErrors in the staging step — the courses crash before they get fully assembled into a stageable record. If the shadow report writer relies on a successful staging step to flush its accumulated diff data, you might get an empty or truncated report from a run where everything crashed. Worth confirming the report writer fires from a try/finally or equivalent at the end of the scrape job, not from inside the staging success path. If you already confirmed this when building the scaffolding, ignore.
Don't be surprised if clean_streak resets between runs even on green runs. The 5-streak counter logic needs to handle the case where a run is clean but a small detail (timestamp, run_id, file path with timestamp suffix) makes the report look "different." The streak logic should only care about is_clean: true, not whether the report contents are identical to the previous report. Easy to get wrong if the streak counter does a naive "this report matches the previous report" check rather than "this report has is_clean=true and the previous one did too."
One run per hour is the spacing rule, but for this validation you might want them closer together. The "1 hour apart" guidance was for catching real cross-run drift (website content changing, AI model updates, etc.) once you're testing actual code changes. For this validation — confirming the no-second-fetch fix actually works — you could run them 5–10 minutes apart and still get the signal you need. The fix either works (5 cleans in a row, regardless of spacing) or it doesn't (first one already dirty). Faster cycle gets you to step 2 today instead of in 5 hours. Switch back to ≥1 hour spacing once you start the actual domestic_only migration in step 2, where website drift becomes a real concern.
Beyond those: go.
Run sequence 1 → 2 → 3 → 4 as you laid out. If run003 is is_clean: true matched: 27, that's a strong signal but I'd actually want to see the full JSON report once before declaring victory — specifically the summary field, the clean_streak/cutover_threshold numbers, and confirm there's no diff payload (zero entries in whatever the diffs/mismatches array is called). Sometimes "clean" can hide a "we couldn't compare these N fields because they're excluded from the comparator" path that masks real issues.
If runs 003-007 all come back clean: green light for step 2, scope the PR strictly to acap_41.yaml + the new_path.py wiring block to read domestic_only from contextvar. Nothing else in that PR.
If any run comes back dirty: post the full report, don't try to fix it before we look at the diff together. The diff payload is the diagnostic — debugging blind risks a wrong fix that masks the real divergence.
Report back with the run003 output.