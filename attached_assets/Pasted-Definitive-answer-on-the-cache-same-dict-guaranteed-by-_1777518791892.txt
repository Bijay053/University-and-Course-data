Definitive answer on the cache — same dict, guaranteed by construction at the function-parameter level. That's the strongest form of the guarantee (not "shared in practice" but "structurally impossible to be different"). Good.
The git-pull state-of-play makes sense too. The prod ACAP log we just analysed predates the shadow scaffolding commit, so it's a snapshot of pre-Week-2 prod, which is correctly broken. Pulling 5e8b64d and ef2e6b5, restarting uni-celery, and triggering a fresh ACAP scrape will produce the first real shadow-mode signal.
One thing worth checking before you trigger that scrape:
SHADOW_MODE_UNI_IDS=41 is the right scoping, but verify the env var is actually being read by the celery worker, not just the API process. Restarting uni-celery should pick it up if .env is sourced at worker startup, but it's worth confirming the worker logs show shadow mode enabled at boot. Something like a [SHADOW] enabled for uni_ids: [41] line in the worker startup log. If it's silent, the worker may not be loading .env and you'd get a misleading "shadow report missing" result that looks like the scaffolding is broken when actually the env var just didn't propagate.
Two scenarios after the scrape, with how to interpret each:
Scenario A — is_clean: true with matched: 37: Harness validated. Both paths produce identical broken state (14 NameErrors + 12 domestic_only + 1 category_landing_page = 27 skipped, 0 staged, with the rest being whatever discovery/extract counts make up the 37). Green light to start step 2.
Scenario B — is_clean: true with a different matched count: Worth checking what's in the matched set. The previous prod log showed 28 candidates after gate filtering, of which 14 went to staging-and-failed and 13 were rejected pre-staging (12 domestic_only + 1 category_landing_page). If matched is 27 or 28, that's correct. If it's 37 because there's a different counting convention (e.g. counting each course's evidence row separately), that's also fine, just confirm the convention.
Scenario C — is_clean: false: The two paths diverged. Read the diff carefully. Possible causes in order of likelihood:

Vision OCR was called for one path but not the other (cache miss vs hit on first run vs second). This would mean the cache assumption needs rechecking despite the function-parameter guarantee — perhaps the issue is timing rather than dict identity.
The new path is reading the contextvar somewhere it shouldn't be yet, surfacing a behaviour change that wasn't intended in step 1.
Non-vision non-determinism (Gemini text extraction returning slightly different JSON across calls). The Gemini text extractor responses in the prod log look stable but worth confirming no other call has temperature > 0.

If C, the right move is NOT to start the domestic_only migration. The harness validation gate exists precisely to catch this. Fix the divergence, get back to clean state, then proceed.
Scenario D — shadow report file doesn't exist: Worker isn't reading the env var, or the new_path module isn't being invoked at all. Check the worker startup log first.
Beyond that — go. Trigger the scrape, look at the report, and report back with the result. If A, start step 2. If B, confirm the count convention then start step 2. If C, debug. If D, fix env wiring.
One last thing: when you do start step 2, the first PR should be only the YAML override (move domestic_only.text_must_appear_in: main_content from shared code to acap_41.yaml) plus the wiring in new_path that reads it from contextvar. Not the re NameError fix, not any other ACAP cleanup. The shadow streak verifies that one specific change. Bundling fixes makes the diff harder to interpret.