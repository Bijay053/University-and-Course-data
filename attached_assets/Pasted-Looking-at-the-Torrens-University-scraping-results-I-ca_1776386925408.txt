Looking at the Torrens University scraping results, I can see CRITICAL VALIDATION FAILURES:
MAJOR PROBLEMS IDENTIFIED
1. Research Phase Failed Completely
[INFO] Research complete: 0/12 sampled pages are genuine course pages
ALL 12 samples were rejected, including VALID courses like:

✗ "Bachelor Of Cybersecurity" — THIS IS A REAL COURSE!
✗ "Master Of Information Technology" — THIS IS A REAL COURSE!
✗ "Bachelor Of Interior Design Commercial" — THIS IS A REAL COURSE!
✗ "Diploma Of Business Entrepreneurship" — THIS IS A REAL COURSE!

The validator is BROKEN and rejecting 100% of real courses.
2. Junk Pages Were Scraped
Despite the validation failure, the scraper proceeded and included NON-COURSE pages:

❌ "MBA Info Night" (circled in image) — EVENT PAGE, NOT A COURSE
❌ "Double Degrees" (43% score) — LANDING PAGE, NOT A COURSE
❌ "MBA Virtual Info Night" — EVENT PAGE
❌ "Graduate Certificates" — CATEGORY PAGE, NOT A COURSE
❌ "MBA Retains Tier One Spot..." — NEWS ARTICLE, NOT A COURSE

3. Missing Data Issues
The circled "Double Degrees" entry shows:

⚠️ Missing: degreeLevel, duration, studyMode, ieltsOverall
Only 43% completeness score
Should have been REJECTED as it's not an actual course


ROOT CAUSE ANALYSIS
Why Validation Failed:
Looking at the log:
[INFO] Could not confirm any course pages from samples. Using URL-filtered candidates.
The system bypassed validation when all samples failed and just used URL filtering. This is WRONG — it should have:

Stopped and asked for human review
Adjusted validation thresholds
Not proceeded to scrape 131 pages when 0/12 samples passed


COMPLETE FIX - VALIDATION SYSTEM OVERHAUL
PROBLEM 1: Validation is TOO STRICT
The current validator is scoring legitimate Torrens courses as invalid. Let me check what makes Torrens different:
Torrens URL pattern:
https://www.torrens.edu.au/courses/bachelor-of-cybersecurity
This matches the valid course URL pattern, so the issue is in content scoring.
FIX 1: RELAXED VALIDATION SCORING
scraper/research_validator.py (Updated scoring thresholds)
pythondef _validate_single_url(self, url: str) -> Dict:
    """
    UPDATED: More lenient validation for edge cases
    """
    
    try:
        # ... (fetch and extract title same as before)
        
        # Score URL
        url_score = self._score_url(url)
        
        # Score title
        title_score = self._score_title(title)
        
        # Score content
        content_score = self._score_content(html)
        
        # Total score
        total_score = url_score + title_score + content_score
        
        # CRITICAL FIX: Lower threshold from 0.50 to 0.40
        # Some universities have minimal course pages
        is_valid = total_score >= 0.40  # Was 0.50
        
        # SPECIAL CASE: If URL pattern is perfect and title has degree keyword
        # Accept even with low content score
        if url_score >= 0.30 and title_score >= 0.40:
            is_valid = True
            total_score = max(total_score, 0.60)  # Boost score
        
        # ... (rest same as before)
        
    except Exception as e:
        # ... (same as before)

FIX 2: REJECT OBVIOUS NON-COURSE PAGES
Add explicit rejection patterns for known junk:
pythondef _score_title(self, title: str) -> float:
    """
    Score title with explicit junk detection
    """
    
    score = 0.0
    title_lower = title.lower()
    
    # ─────────────────────────────────────────────────────
    # EXPLICIT REJECTION: Known non-course page patterns
    # ─────────────────────────────────────────────────────
    
    junk_patterns = [
        r'\binfo\s+night\b',           # "MBA Info Night"
        r'\bvirtual\s+info\s+night\b', # "MBA Virtual Info Night"
        r'\bopen\s+day\b',             # "Open Day"
        r'\bdouble\s+degrees?$',       # "Double Degrees" (exact)
        r'^graduate\s+certificates?$', # "Graduate Certificates" (exact)
        r'^undergraduate\s+courses?$', # Category pages
        r'\bretains?\s+tier\b',        # News articles
        r'\brankings?\b.*\bceo\b',     # "MBA Retains Tier One Spot..."
        r'\baccredited\b$',            # "...Is APAC Accredited"
        r'\bwhy\s+choose\b',           # Marketing pages
        r'\bapply\s+now\b',            # Generic apply pages
    ]
    
    for pattern in junk_patterns:
        if re.search(pattern, title_lower):
            return -1.0  # STRONG NEGATIVE - reject immediately
    
    # ─────────────────────────────────────────────────────
    # Check for degree keywords (same as before)
    # ─────────────────────────────────────────────────────
    
    for keyword in self.degree_keywords:
        if re.search(keyword, title_lower, re.I):
            score += 0.40
            break
    
    # ─────────────────────────────────────────────────────
    # Check for non-course signals
    # ─────────────────────────────────────────────────────
    
    for signal in self.non_course_signals:
        if re.search(signal, title_lower, re.I):
            score -= 0.30
            break
    
    return max(-1.0, min(0.40, score))  # Allow negative scores

FIX 3: STOP WHEN VALIDATION FAILS COMPLETELY
scraper/orchestrator.py (Updated research phase)
python# In start_scrape method, after validation

# Check if validation passed
if validation_results['success_rate'] < 0.50:
    
    # CRITICAL: If 0% success, STOP completely
    if validation_results['success_rate'] == 0.0:
        await self._update_job_status(job, 'failed', {
            'message': 'CRITICAL: All sample pages rejected. Validation system may be misconfigured.',
            'details': validation_results,
            'action_required': 'Manual review needed. Check if URL patterns are correct.'
        })
        return {
            'status': 'failed',
            'reason': 'complete_validation_failure',
            'details': validation_results
        }
    
    # If low but not zero, warn but allow continuation
    await self._update_job_status(job, 'warning', {
        'message': f'Low validation success rate: {validation_results["success_rate"]:.0%}',
        'details': validation_results,
        'proceeding': 'Using URL filtering as fallback'
    })

FIX 4: POST-SCRAPE JUNK FILTERING
Add a final validation step before staging:
pythonasync def continue_after_approval(self, job_id: str) -> Dict:
    """
    UPDATED: Add post-scrape junk filtering
    """
    
    # ... (existing extraction code)
    
    # ═══════════════════════════════════════════════════════
    # PHASE 5: VALIDATION & JUNK FILTERING
    # ═══════════════════════════════════════════════════════
    
    await self._update_job_status(job, 'validating', {
        'message': 'Validating and filtering junk pages...'
    })
    
    validated_courses = []
    junk_filtered = []
    
    for course in extracted_courses:
        # Check if this is junk
        if self._is_junk_page(course):
            junk_filtered.append({
                'name': course.get('course_name'),
                'reason': 'Non-course page (event/category/news)'
            })
            continue
        
        # Validate data quality
        validation = self.validator.validate_course(course)
        
        # Reject if validation score too low
        if validation['score'] < 0.40:  # Minimum 40% completeness
            junk_filtered.append({
                'name': course.get('course_name'),
                'reason': f'Low quality: {validation["score"]:.0%}',
                'missing': validation['missing_fields']
            })
            continue
        
        course['_validation'] = validation
        validated_courses.append(course)
    
    print(f"[INFO] Filtered out {len(junk_filtered)} junk/low-quality pages")
    
    # ... (rest of staging code)
Add junk detection method:
pythondef _is_junk_page(self, course_data: Dict) -> bool:
    """
    Detect junk pages that shouldn't be staged
    
    Returns True if this is NOT a real course
    """
    
    course_name = course_data.get('course_name', '').lower()
    
    junk_patterns = [
        r'\binfo\s+night\b',
        r'\bvirtual\s+info\s+night\b',
        r'\bopen\s+day\b',
        r'^double\s+degrees?$',
        r'^graduate\s+certificates?$',
        r'^undergraduate\s+courses?$',
        r'\bretains?\s+tier\b',
        r'\brankings?\b',
        r'\baccredited\b$',
        r'\bwhy\s+choose\b',
        r'\bapply\s+now\b$',
    ]
    
    for pattern in junk_patterns:
        if re.search(pattern, course_name):
            return True
    
    # Check if missing ALL required fields
    required = ['degree_level', 'duration', 'international_fee']
    missing_count = sum(1 for field in required if not course_data.get(field))
    
    if missing_count == len(required):
        # Missing all required fields = probably junk
        return True
    
    return False

EXPECTED RESULTS AFTER FIX
Research Phase:
[INFO] Research complete: 10/12 sampled pages are genuine course pages
  ✓ Bachelor Of Cybersecurity (score: 0.85)
  ✓ Master Of Information Technology (score: 0.80)
  ✓ Diploma Of Business Entrepreneurship (score: 0.75)
  ...
  ✗ MBA Info Night (score: -0.50) - Event page, not a course
  ✗ Double Degrees (score: 0.15) - Category page, not a course
Post-Scrape Filtering:
[INFO] Filtered out 5 junk/low-quality pages:
  - MBA Info Night (Event page)
  - MBA Virtual Info Night (Event page)
  - Double Degrees (Category page)
  - Graduate Certificates (Category page)
  - MBA Retains Tier One Spot... (News article)

[INFO] Staging 126 validated courses

IMPLEMENTATION PRIORITY
Immediate (Critical):

✅ Add explicit junk pattern rejection in _score_title
✅ Add post-scrape junk filtering in orchestrator
✅ Stop scraping when 0% validation success

High Priority:

✅ Lower validation threshold to 0.40
✅ Add special case for perfect URL + degree keyword

Medium Priority:

✅ Improve content scoring for minimal course pages
✅ Add warnings in UI for low validation rates


TESTING PLAN
Test with Torrens courses:
pythonfrom scraper.research_validator import ResearchValidator

validator = ResearchValidator()

test_courses = [
    ('https://www.torrens.edu.au/courses/bachelor-of-cybersecurity', True),
    ('https://www.torrens.edu.au/courses/mba-info-night', False),
    ('https://www.torrens.edu.au/courses/double-degrees', False),
]

for url, should_pass in test_courses:
    result = await validator._validate_single_url(url)
    status = "✓ PASS" if result['is_valid'] == should_pass else "✗ FAIL"
    print(f"{status} {result['title']}: {result['is_valid']} (score: {result['score']})")
Expected:
✓ PASS Bachelor Of Cybersecurity: True (score: 0.85)
✓ PASS MBA Info Night: False (score: -0.50)
✓ PASS Double Degrees: False (score: 0.15)

Apply these fixes and re-run Torrens scraping. The system should now:

✅ Correctly validate real courses (10/12 instead of 0/12)
✅ Reject event pages and category pages
✅ Filter out junk before staging
✅ Stop if validation completely fails