Claude responded: Looking at the log carefully — your fix didn't ship.Looking at the log carefully — your fix didn't ship. The 4 KBS grad cert courses still show Duration: ✅ '8.0' [regex conf=0.75] in the FIELD SUMMARY, with the exact same conf=0.75 as before. Nothing changed.
But here's the more important thing the log reveals: the diagnosis from the previous session was wrong. Look at the actual page text the scraper extracted:
Graduate Certificate in Accounting
Typical Duration / Standard Study Option 8 months / 4 subjects / 2 trimesters
That's the entire duration sentence. There's no "complete their qualification within 8 years" anywhere in the visible text. The string "8 years" doesn't appear in the GP-DEBUG dump for any of the 4 grad cert pages (Accounting, Business Administration, Business Analytics, Information Technology). They all just say 8 months / 4 subjects / 2 trimesters.
So the previous session's "completion deadline with intervening words" theory was a hallucination — it patched a regex against a sentence that isn't on the page. That's why deploying the fix would change nothing even if it had shipped.
What's actually happening is much simpler and more interesting. Look at where the regex picks up 8.0:

Page text: 8 months / 4 subjects / 2 trimesters
Regex output: Duration: ✅ '8.0' [regex conf=0.75]
Final stored value: 8.0 Year

The regex is correctly grabbing the number 8 from "8 months" — but somewhere downstream, the unit is being either dropped, misread, or coerced to Year. The Gemini AI fallback gets it right ("duration_text": "8 months", duration_value: 0.67, duration_unit: "years" — i.e. 8 months expressed as 0.67 years), but the regex path stores 8.0 Year. So the regex is reading the integer and the unit-handling code on that path is either:

Defaulting to Year when it can't parse the unit, or
Not reading the unit token at all and assuming Year, or
Reading months correctly but a downstream conversion is wrong

The smoking gun for option 2 or 3: the Master of Professional Accounting page text says 1 year, 8 months and the regex stores 20.0 — that's 1×12 + 8 = 20 months correctly summed, but then stored with unit Year (which is why the sanity check nullifies it: "20.0 yrs exceeds sanity limit"). The regex is computing months, the storage is labeling it years.
Same pattern for Master of Business Analytics: page says 1 year, 8 months → regex gets 20.0 → stored as Year → nullified.
And Graduate Diploma of Business Administration: page says 1 year, 4 months → regex gets 16.0 → stored as Year → nullified.
So this isn't a "completion deadline phrase" bug at all. The regex extractor is computing total months correctly when it sees compound durations, but the value is being persisted with the wrong unit label. The grad certs say 8 months (single token), regex extracts 8, stores as 8.0 Year — same bug, simpler input.
What to actually look at:

The regex path that produces [regex conf=0.75] — find where it returns (value, unit) and check whether the unit field is being read from the matched months token or hardcoded.
The [NULLIFIED] log lines confirm a sanity-limit guard exists, but it only catches values ≥ ~10 years. Anything 5–9 (like 8.0 Year) slips through. That's why grad certs persist as bad data while masters get nullified.
The Gemini fallback returns the right answer — but [FALLBACK] AI filled 2 field(s) ... duration_value, duration_unit only fires when the field is missing, not when it's already populated (incorrectly) by the regex. So the AI never gets a chance to overwrite the wrong regex value.

Two-line fix direction:

The real bug is in the regex unit assignment, not the anti-context regex. Find the function emitting [regex conf=0.75] for duration and check the unit branch — it's almost certainly defaulting months → Year due to a bad mapping or a missing case.
Tightening the sanity limit (anything ≥ 5 with unit Year and degree level grad cert/grad diploma should auto-flip to Month or trigger AI fallback) would catch this even without finding the root cause.

Want to grep the codebase for where the duration regex assigns its unit? If you can paste the function that emits [regex conf=0.75] (probably in the same file as _PATTERNS), I can point at the exact line.